export enum GENDER {
    MALE = 'male',
    FEMALE = 'female',
    OTHER = 'other',
}

export enum ROLE {
    ADMIN = 'admin',
    USER = 'user',
    INTRUCTOR = 'instructor',
}

export enum ORDER_STATUS {
    PENDING = 'pending',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    INIT = 'init',
        
}